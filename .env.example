# Create a free key at https://open-platform.theguardian.com/access/
# The key is read only on the server and must never be prefixed with NEXT_PUBLIC_.
GUARDIAN_API_KEY=

# Optional local visual testing without an external API call.
# Keep false/omit in the submitted deployment so the Guardian API is used.
NEWS_USE_FIXTURE=false
