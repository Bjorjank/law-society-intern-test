Run this from the project root on Windows to download the exact supplied Figma assets:

npm run figma:assets

Then verify:

npm run figma:verify

The URLs in scripts/download-figma-assets.ps1 are short-lived Figma MCP export URLs.
